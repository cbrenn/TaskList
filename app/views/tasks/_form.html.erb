<%= form_for @task do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :status %>
<%= f.text_field :status %>

<%= f.label :priority %>
<%= f.text_field :priority %>

<%= f.label :start_date %>
<%= f.text_field :start_date %>

<%= f.submit "Add changes" %>
<% end %>

<li>
 <%= link_to("I've changed my mind, take me back to the list.", tasks_path) %>
</li>
